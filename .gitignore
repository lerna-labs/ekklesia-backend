.DS_Store
node_modules
/public
/backup/data
/scripts
/logs
/preprod-logs
/mainnet-logs
/.vscode
deploy*.sh
npm-debug.log
yarn-error.log

.env*
!.env.example

# Keep repo .npmrc (env-var substitution only, no literal token),
# but exclude any local override that might contain a token.
.npmrc.local