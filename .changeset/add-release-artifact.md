---
"ekklesia-backend": patch
---

Publish a versioned source tarball with each GitHub Release, containing just the files needed to run the server in production. Pushes to staging also publish a rolling preprod build so the testnet can track the release candidate.
